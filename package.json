{
  "name": "cubiczo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite build && vite preview --port 3000 --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "start": "vite preview  --port 3000 --host 0.0.0.0"
  },
  "dependencies": {
    "vite": "^6.2.3"
  }
}
